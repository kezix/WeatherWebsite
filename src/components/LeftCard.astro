---
// export interface Props {
// 	title: string;
// 	body: string;
// 	href: string;
// }

// const { href, title, body } = Astro.props;
---
    <div class="w-[699px] h-max flex flex-col mr-0 md:mr-24">
        <p class="relative font-sans text-5xl font-extrabold text-blurple text-shadow-blurple mb-8">Welcome!</p>
        <p class="text-2xl relative font-sans font-semibold text-white mb-8">This website supports only a few countries because it uses its own API, to which data is sent by volunteers who support the site.</p>
        <hr class="line relative w-16 text-white mb-8"/>
        <p class="text-2xl relative font-sans font-semibold text-white mb-8">The weather.kezix.me API is available to the public. Instructions for use are available <a class="font-sans text-blurple font-extrabold text-shadow-blurple">here</a>. <a class="font-sans text-red font-medium text-shadow-red text-base">(soon!)</a></p>
    </div>

<style>
    hr{
        padding: 0px;
        margin: 0px;
    }
</style>
